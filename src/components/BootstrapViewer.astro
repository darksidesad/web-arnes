---
import { bootstrapSections } from '../data/bootstrapPrompt';
const tabs = Object.keys(bootstrapSections);
---

<div class="relative">
  <div class="mb-4 flex flex-wrap gap-2 border-b border-border" role="tablist">
    {tabs.map((tab, i) => (
      <button
        class={`tab-btn ${i === 0 ? 'is-active' : ''}`}
        data-tab={tab}
        role="tab"
        aria-selected={i === 0 ? 'true' : 'false'}
      >
        {tab}
      </button>
    ))}
  </div>

  <div class="relative">
    {tabs.map((tab, i) => (
      <div
        class={`tab-content ${i === 0 ? 'block' : 'hidden'}`}
        data-tab-content={tab}
        role="tabpanel"
      >
        <div class="code-block max-w-full overflow-x-auto">
          <button
            class="copy-btn btn-ghost absolute top-3 right-3"
            data-copy={bootstrapSections[tab]}
          >
            Copiar
          </button>
          <pre class="text-sm leading-relaxed whitespace-pre-wrap"><code>{bootstrapSections[tab]}</code></pre>
        </div>
      </div>
    ))}
  </div>
</div>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const tabBtns = document.querySelectorAll('.tab-btn');
    const tabContents = document.querySelectorAll('.tab-content');
    const copyBtns = document.querySelectorAll('.copy-btn');

    tabBtns.forEach((btn) => {
      btn.addEventListener('click', () => {
        const tabId = btn.getAttribute('data-tab');

        tabBtns.forEach((b) => {
          b.classList.remove('is-active');
          b.setAttribute('aria-selected', 'false');
        });

        btn.classList.add('is-active');
        btn.setAttribute('aria-selected', 'true');

        tabContents.forEach((content) => {
          const isMatch = content.getAttribute('data-tab-content') === tabId;
          content.classList.toggle('hidden', !isMatch);
          content.classList.toggle('block', isMatch);
        });
      });
    });

    copyBtns.forEach((btn) => {
      btn.addEventListener('click', () => {
        const text = btn.getAttribute('data-copy');
        if (text) {
          navigator.clipboard.writeText(text).then(() => {
            const originalText = btn.textContent;
            btn.textContent = 'Copiado!';
            btn.classList.add('is-copied');
            setTimeout(() => {
              btn.textContent = originalText;
              btn.classList.remove('is-copied');
            }, 2000);
          });
        }
      });
    });
  });
</script>
